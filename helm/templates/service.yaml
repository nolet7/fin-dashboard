apiVersion: v1
kind: Service
metadata:
  name: {{ include "financehub.fullname" . }}
  labels:
    {{- include "financehub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "financehub.selectorLabels" . | nindent 4 }}